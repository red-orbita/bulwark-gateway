{{- if .Values.ingress.enabled }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "bulwark-gateway.fullname" . }}
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.labels" . | nindent 4 }}
  annotations:
    {{- if .Values.ingress.tls.certManager }}
    cert-manager.io/cluster-issuer: {{ .Values.ingress.tls.issuerName | quote }}
    {{- end }}
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.hosts.proxy }}
        - {{ .Values.ingress.hosts.admin }}
      secretName: {{ .Values.ingress.tls.secretName }}
  {{- end }}
  rules:
    # Proxy API — main guardrail path
    - host: {{ .Values.ingress.hosts.proxy }}
      http:
        paths:
          - path: /v1
            pathType: Prefix
            backend:
              service:
                name: proxy
                port:
                  number: 8080
          - path: /health
            pathType: Prefix
            backend:
              service:
                name: proxy
                port:
                  number: 8080
    # Admin portal — dedicated subdomain
    # SECURITY FIX (APT-15): Grafana moved from proxy host to admin host.
    # Previously any LLM client could reach Grafana at /grafana on the proxy domain.
    - host: {{ .Values.ingress.hosts.admin }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin
                port:
                  number: 8090
          {{- if .Values.monitoring.grafana.enabled }}
          - path: /grafana
            pathType: Prefix
            backend:
              service:
                name: grafana
                port:
                  number: 3000
          {{- end }}
{{- end }}
