{{- if .Values.mtls.enabled }}
{{- /*
  mTLS Secrets for inter-service communication (proxy ↔ admin).
  
  Three modes:
    1. Auto-generated (default): Helm generates self-signed certs on install
    2. cert-manager: Creates Certificate resources for automatic renewal
    3. Pre-existing secrets: References customer-provided secrets
*/ -}}

{{- if and (not .Values.mtls.certManager) (not .Values.mtls.existingSecrets.ca) }}
{{- /*
  Mode 1: Auto-generated certificates (self-signed CA + service certs).
  NOTE: These are generated once at install time. Use cert-manager for rotation.
*/ -}}
---
# mTLS CA Certificate (shared trust anchor)
apiVersion: v1
kind: Secret
metadata:
  name: bulwark-mtls-ca
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
  annotations:
    helm.sh/resource-policy: keep
    bulwark-gateway.io/purpose: "Internal CA for inter-service mTLS"
type: Opaque
data:
  # CA certificate and key are generated externally via scripts/generate-mtls-certs.sh
  # This secret is a placeholder — populate it before enabling mTLS:
  #   kubectl create secret generic bulwark-mtls-ca \
  #     --from-file=ca.crt=./certs/mtls/ca.crt \
  #     -n bulwark-gateway
  ca.crt: {{ "" | b64enc | quote }}
---
# Proxy Server TLS Certificate
apiVersion: v1
kind: Secret
metadata:
  name: bulwark-proxy-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
  annotations:
    helm.sh/resource-policy: keep
type: kubernetes.io/tls
data:
  tls.crt: {{ "" | b64enc | quote }}
  tls.key: {{ "" | b64enc | quote }}
---
# Admin Server TLS Certificate
apiVersion: v1
kind: Secret
metadata:
  name: bulwark-admin-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.admin.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
  annotations:
    helm.sh/resource-policy: keep
type: kubernetes.io/tls
data:
  tls.crt: {{ "" | b64enc | quote }}
  tls.key: {{ "" | b64enc | quote }}
---
# Proxy Client Certificate (for proxy→admin calls)
apiVersion: v1
kind: Secret
metadata:
  name: bulwark-proxy-client-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
  annotations:
    helm.sh/resource-policy: keep
type: kubernetes.io/tls
data:
  tls.crt: {{ "" | b64enc | quote }}
  tls.key: {{ "" | b64enc | quote }}
---
# Admin Client Certificate (for admin→proxy calls)
apiVersion: v1
kind: Secret
metadata:
  name: bulwark-admin-client-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.admin.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
  annotations:
    helm.sh/resource-policy: keep
type: kubernetes.io/tls
data:
  tls.crt: {{ "" | b64enc | quote }}
  tls.key: {{ "" | b64enc | quote }}
{{- end }}

{{- if .Values.mtls.certManager }}
{{- /*
  Mode 2: cert-manager integration.
  Creates Certificate resources that are automatically issued and renewed.
  Requires cert-manager to be installed in the cluster.
*/ -}}
---
# Internal CA Issuer (self-signed root for inter-service certs)
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: bulwark-selfsigned-issuer
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  selfSigned: {}
---
# CA Certificate (used as root for signing service certs)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bulwark-internal-ca
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  isCA: true
  commonName: {{ .Values.mtls.issuerName }}
  secretName: bulwark-mtls-ca
  duration: {{ printf "%dh" (int .Values.mtls.certValidity) }}
  renewBefore: 720h  # 30 days before expiry
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: bulwark-selfsigned-issuer
    kind: Issuer
---
# CA Issuer (signs service certificates using the internal CA)
apiVersion: cert-manager.io/v1
kind: Issuer
metadata:
  name: {{ .Values.mtls.issuerName }}
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  ca:
    secretName: bulwark-mtls-ca
---
# Proxy Server Certificate
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bulwark-proxy-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  secretName: bulwark-proxy-mtls
  duration: {{ printf "%dh" (int .Values.mtls.certValidity) }}
  renewBefore: 720h
  commonName: proxy.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
  dnsNames:
    - proxy.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
    - proxy.{{ include "bulwark-gateway.namespace" . }}.svc
    - proxy.{{ include "bulwark-gateway.namespace" . }}
    - proxy
    - localhost
    - bulwark-proxy
  ipAddresses:
    - "127.0.0.1"
    - "::1"
  usages:
    - server auth
    - digital signature
    - key encipherment
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: {{ .Values.mtls.issuerName }}
    kind: Issuer
---
# Admin Server Certificate
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bulwark-admin-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.admin.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  secretName: bulwark-admin-mtls
  duration: {{ printf "%dh" (int .Values.mtls.certValidity) }}
  renewBefore: 720h
  commonName: admin.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
  dnsNames:
    - admin.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
    - admin.{{ include "bulwark-gateway.namespace" . }}.svc
    - admin.{{ include "bulwark-gateway.namespace" . }}
    - admin
    - localhost
    - bulwark-admin
  ipAddresses:
    - "127.0.0.1"
    - "::1"
  usages:
    - server auth
    - digital signature
    - key encipherment
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: {{ .Values.mtls.issuerName }}
    kind: Issuer
---
# Proxy Client Certificate (for proxy→admin calls)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bulwark-proxy-client-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.proxy.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  secretName: bulwark-proxy-client-mtls
  duration: {{ printf "%dh" (int .Values.mtls.certValidity) }}
  renewBefore: 720h
  commonName: proxy.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
  dnsNames:
    - proxy.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
    - proxy.{{ include "bulwark-gateway.namespace" . }}
    - bulwark-proxy
  usages:
    - client auth
    - digital signature
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: {{ .Values.mtls.issuerName }}
    kind: Issuer
---
# Admin Client Certificate (for admin→proxy calls)
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: bulwark-admin-client-mtls
  namespace: {{ include "bulwark-gateway.namespace" . }}
  labels:
    {{- include "bulwark-gateway.admin.labels" . | nindent 4 }}
    app.kubernetes.io/component: mtls
spec:
  secretName: bulwark-admin-client-mtls
  duration: {{ printf "%dh" (int .Values.mtls.certValidity) }}
  renewBefore: 720h
  commonName: admin.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
  dnsNames:
    - admin.{{ include "bulwark-gateway.namespace" . }}.svc.cluster.local
    - admin.{{ include "bulwark-gateway.namespace" . }}
    - bulwark-admin
  usages:
    - client auth
    - digital signature
  privateKey:
    algorithm: ECDSA
    size: 256
  issuerRef:
    name: {{ .Values.mtls.issuerName }}
    kind: Issuer
{{- end }}

{{- if .Values.mtls.existingSecrets.ca }}
{{- /*
  Mode 3: Pre-existing secrets.
  Customer provides their own CA and certificates.
  This section only documents the expected secret format — no resources created.
  
  Expected secrets:
    - {{ .Values.mtls.existingSecrets.ca }}:        key "ca.crt"
    - {{ .Values.mtls.existingSecrets.proxyCert }}: keys "tls.crt", "tls.key"
    - {{ .Values.mtls.existingSecrets.adminCert }}: keys "tls.crt", "tls.key"
*/ -}}
{{- end }}
{{- end }}
