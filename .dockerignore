# ============================================================
# Sentinel Gateway — .dockerignore
# Excludes files not needed inside container images.
# ============================================================

# --- Version control ---
.git
.gitignore

# --- Python cache / build ---
.venv
venv
__pycache__
*.pyc
*.pyo
.mypy_cache
.pytest_cache
.ruff_cache
*.egg-info
dist
build

# --- Tests and docs (not needed at runtime) ---
tests/
docs/
*.md

# --- Infrastructure / deployment (not needed in container) ---
helm/
k8s/
ci/
.github/
scripts/
docker-compose.*
Dockerfile
docker/
# Exception: the proxy launcher is baked into the image (distroless has no shell;
# derives the uvicorn worker count from BULWARK_WORKERS — GAP-A).
!docker/proxy_launcher.py
# Kept for the (still Debian-based) admin image and any shell-based runtime.
!docker/entrypoint-proxy.sh

# --- Secrets (injected at runtime via mounts) ---
secrets/
.env
.env.*

# --- Runtime data dirs (mounted at runtime) ---
data/
reports/
shared/

# --- IDE / Editor ---
.vscode/
.idea/
*.swp
*~

# --- Agent tooling ---
.opencode/

# --- OS artifacts ---
.DS_Store
Thumbs.db
